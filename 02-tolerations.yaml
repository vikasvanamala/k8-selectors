apiVersion: apps/v1
kind: Deployment
metadata: 
  name: taints-tolerations-demo
  labels:
    purpose: taints-tolerations
    project: roboshop 
spec:
  replicas: 1
  selector:
    matchLabels:
      purpose: taints-tolerations
      project: roboshop
  template:
    metadata:
      labels:
        purpose: taints-tolerations
        project: roboshop
    spec:
      nodeSelector:
        kubernetes.io/hostname: ip-192-168-30-56.ec2.internal 
      containers:
      - name: nginx
        image: nginx:latest
      tolerations:
      - key: "project"
        operator: "Equal"
        value: "roboshop"
        effect: "NoSchedule"